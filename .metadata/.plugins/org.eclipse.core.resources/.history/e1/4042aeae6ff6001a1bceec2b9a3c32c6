package CS;

public class HeureSimple {
	private int H;
	private int M;
	
	public HeureSimple(int H, int M) {
		if (H < 24 && H >= 0) {
			if (M % 5 < 60 && M % 5 >= 0) {
				this.H = H;
				this.M = M;
			}
		}
	}
}
